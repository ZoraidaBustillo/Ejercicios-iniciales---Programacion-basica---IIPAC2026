#include <iostream>      // Entrada/salida estándar: cin, cout, cerr
#include <vector>         // Contenedor dinámico que crece automáticamente
#include <string>         // Manejo de cadenas de caracteres
#include <algorithm>      // Funciones de algoritmo: sort, find, etc.
#include <cmath>          // Funciones matemáticas: sqrt, sin, cos, etc.
#include <fstream>        // Lectura y escritura de archivos
#include <map>            // Contenedor asociativo clave-valor ordenado
#include <set>            // Contenedor de elementos únicos ordenados
#include <queue>          // Estructura cola (FIFO)
#include <stack>          // Estructura pila (LIFO)
#include <cstring>        // Funciones para cadenas de caracteres estilo C
#include <iomanip>        // Manipuladores para formato de entrada/salida
#include <random>         // Generación de números aleatorios
#include <memory>         // Punteros inteligentes: unique_ptr, shared_ptr
#include <chrono>         // Medición de tiempo y duraciones

int main() {
    std::cout << "Librerias cargadas correctamente" << std::endl;
    return 0;
}

